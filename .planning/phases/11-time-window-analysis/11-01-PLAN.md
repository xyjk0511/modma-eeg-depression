---
phase: 11-time-window-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - run_modma_erp.py
autonomous: true
requirements:
  - TWIN-01
  - TWIN-02

must_haves:
  truths:
    - "out_phase11/ttest_curve.png exists: t-stat vs time plot covering 250-500ms with FDR-significant points marked"
    - "out_phase11/bin_ablation.csv exists with 5 rows (one per 50ms bin) plus header"
    - "Ablation table printed to stdout with Bin | BA | p-value | Sig | Note columns, best BA marked"
    - "Peak |t-stat| time point annotated on plot"
  artifacts:
    - path: "run_modma_erp.py"
      provides: "load_erp_timeseries, plot_ttest_curve, run_bin_ablation, run_time_window_analysis"
      contains: "load_erp_timeseries"
    - path: "out_phase11/ttest_curve.png"
      provides: "t-stat vs time visualization"
    - path: "out_phase11/bin_ablation.csv"
      provides: "50ms bin ablation results table"
  key_links:
    - from: "run_time_window_analysis()"
      to: "load_erp_timeseries()"
      via: "returns avg_erps array retained per subject"
      pattern: "load_erp_timeseries"
    - from: "run_bin_ablation()"
      to: "permutation_test_subset(X_bin, y, C=0.1)"
      via: "C parameter forwarded through permutation_test_subset -> _loso_ba_subset"
      pattern: "C=0.1"
---

<objective>
逐时间点 t-test 描述性分析（TWIN-01）+ 50ms bins 消融分类（TWIN-02），识别 P300 窗口内最具判别力的时间段。

Purpose: 补充 Phase 10 的空间分析（哪些通道），从时间维度回答哪个 50ms 段信号最强。
Output: out_phase11/ttest_curve.png + out_phase11/bin_ablation.csv + stdout 消融表格。
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@run_modma_erp.py
@.planning/phases/10-electrode-selection/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: add load_erp_timeseries() and C parameter to _loso_ba_subset / permutation_test_subset</name>
  <files>run_modma_erp.py</files>
  <action>
Two changes to run_modma_erp.py:

1. Add load_erp_timeseries() after load_erp_features(). Same loop but retains avg_erp
   per subject and returns times array. Return: (X_feat, avg_erps, times, y, ids)
   where avg_erps shape is (N, 128, n_times).

   Key notes:
   - times_ref = epochs.times.copy() from first valid subject only
   - Assert avg_erp.shape[1] == len(times_ref) for each subsequent subject
   - avg_erp = data.mean(axis=0)  # (128, n_times)
   - X_feat = same 128-dim P300 mean as load_erp_features

   Skeleton:
     def load_erp_timeseries(condition="hcue"):
         global CONDITION
         CONDITION = condition
         raw_files = sorted(ERP_DIR.glob("*.raw"))
         all_feat, all_erps, all_labels, all_ids = [], [], [], []
         times_ref = None
         for fpath in raw_files:
             # ... same per-subject logic as load_erp_features ...
             avg_erp = data.mean(axis=0)
             if times_ref is None:
                 times_ref = epochs.times.copy()
             assert avg_erp.shape[1] == len(times_ref), "times mismatch"
             p300_mask = (times_ref >= P300_WIN[0]) & (times_ref <= P300_WIN[1])
             feat = avg_erp[:, p300_mask].mean(axis=1)
             all_feat.append(feat)
             all_erps.append(avg_erp)
             all_labels.append(label)
             all_ids.append(sub_id)
         return (np.array(all_feat), np.array(all_erps),
                 times_ref, np.array(all_labels), np.array(all_ids))

2. Add C=1.0 parameter to _loso_ba_subset and permutation_test_subset (backward compatible).

   _loso_ba_subset(X, y, C=1.0): update LogisticRegression(C=C, ...)
   permutation_test_subset(X, y, n_perm=1000, seed=42, C=1.0):
     - obs_ba = _loso_ba_subset(X, y, C=C)
     - one_perm closure: return _loso_ba_subset(X, rng.permutation(y), C=C)
  </action>
  <verify>python -c "import ast; ast.parse(open('run_modma_erp.py').read()); print('syntax ok')"</verify>
  <done>Syntax check passes; "def load_erp_timeseries" and "def _loso_ba_subset(X, y, C=1.0)" present in run_modma_erp.py</done>
</task>

<task type="auto">
  <name>Task 2: add plot_ttest_curve, run_bin_ablation, run_time_window_analysis, wire into __main__</name>
  <files>run_modma_erp.py</files>
  <action>
Add imports, BINS constant, three functions, and __main__ call.

1. Imports (add at top if not present):
     import csv
     from scipy.stats import ttest_ind, false_discovery_control

2. BINS constant (module level):
     BINS = [
         ("250-300ms", 0.250, 0.300),
         ("300-350ms", 0.300, 0.350),
         ("350-400ms", 0.350, 0.400),
         ("400-450ms", 0.400, 0.450),
         ("450-500ms", 0.450, 0.500),
     ]

3. plot_ttest_curve(avg_erps, times, y, out_dir):
   - parietal_idx = list(get_parietal_indices().values())
   - mask_time = (times >= 0.25) & (times <= 0.50)
   - erp_par = avg_erps[:, parietal_idx, :][:, :, mask_time].mean(axis=1)  # (N, n_t)
   - Per-timepoint ttest_ind(equal_var=False) for mdd vs hc
   - adj_p = false_discovery_control(p_vals)  # returns adjusted p-values, NOT booleans
   - sig_mask = adj_p < 0.05
   - peak_t = t_times[np.argmax(np.abs(t_stats))]
   - Plot: steelblue t-stat line, gray axvspan per bin (alpha=0.15),
     red scatter for sig points, orange axvline for peak
   - Save to Path(out_dir)/"ttest_curve.png" dpi=150, plt.close()
   - Print: peak time and FDR-sig count

4. run_bin_ablation(avg_erps, times, y, out_dir):
   - Loop BINS; half-open intervals (times >= t0) & (times < t1) except last bin uses <=
   - X_bin = avg_erps[:, :, mask].mean(axis=2)  # (N, 128)
   - ba, p = permutation_test_subset(X_bin, y, n_perm=1000, C=0.1)
   - Print table: Bin | BA | p-value | Sig | Note (best BA gets "star")
   - Save CSV to Path(out_dir)/"bin_ablation.csv" with header row

5. run_time_window_analysis():
     def run_time_window_analysis():
         print("\n" + "="*50, flush=True)
         print("Phase 11: Time-Window Analysis", flush=True)
         print("="*50, flush=True)
         out_dir = Path("out_phase11")
         out_dir.mkdir(exist_ok=True)
         _, avg_erps, times, y, _ = load_erp_timeseries(condition="hcue")
         print(f"  avg_erps shape: {avg_erps.shape}", flush=True)
         print("\nTWIN-01: Per-timepoint t-test curve", flush=True)
         plot_ttest_curve(avg_erps, times, y, out_dir)
         print("\nTWIN-02: 50ms bin ablation (128-dim, C=0.1)", flush=True)
         run_bin_ablation(avg_erps, times, y, out_dir)

6. In __main__, after the Phase 10 block, append:
     run_time_window_analysis()
  </action>
  <verify>python -c "import ast; ast.parse(open('run_modma_erp.py').read()); print('syntax ok')" &amp;&amp; python run_modma_erp.py 2&gt;&amp;1 | grep -E "(Phase 11|TWIN|Saved|Peak|250-300|bin_ablation)"</verify>
  <done>
  - Syntax check passes
  - "Phase 11: Time-Window Analysis" in output
  - "Saved: out_phase11/ttest_curve.png" in output
  - "Saved: out_phase11/bin_ablation.csv" in output
  - 5 bin rows printed to stdout
  - Both files exist on disk
  </done>
</task>

</tasks>

<verification>
Run: python run_modma_erp.py 2>&1 | grep -E "(Phase 11|TWIN|Saved|Peak|250-300|300-350|350-400|400-450|450-500)"

Expected:
1. "Phase 11: Time-Window Analysis"
2. "TWIN-01: Per-timepoint t-test curve" + "Saved: out_phase11/ttest_curve.png"
3. "TWIN-02: 50ms bin ablation" + 5 bin lines with BA/p-value
4. "Saved: out_phase11/bin_ablation.csv"

File checks:
  ls out_phase11/          # shows ttest_curve.png and bin_ablation.csv
  head out_phase11/bin_ablation.csv  # header + 5 data rows
</verification>

<success_criteria>
- TWIN-01: out_phase11/ttest_curve.png exists; covers 250-500ms; FDR-significant points and peak |t-stat| annotated
- TWIN-02: out_phase11/bin_ablation.csv has 5 bins with BA and p-value; best BA bin marked in stdout table
</success_criteria>

<output>
After completion, create .planning/phases/11-time-window-analysis/11-01-SUMMARY.md
</output>
