---
phase: 03-feature-permutation-opt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modma_mdd_real_experiment.py
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - "extract_features returns exactly 29 dimensions per window"
    - "Theta/Beta ratio computed per region (5 dims)"
    - "Alpha asymmetry includes frontal and temporal (2 dims)"
    - "Only top-2 Riemannian features computed"
    - "Pipeline uses SVM + Logistic only"
    - "run_simplified_cv works on pre-extracted features"
  artifacts:
    - path: "modma_mdd_real_experiment.py"
      provides: "29-dim extract_features, run_simplified_cv"
      contains: "theta_beta_ratio"
  key_links:
    - from: "extract_features"
      to: "band_power"
      via: "theta/beta ratio"
      pattern: "theta.*beta.*1e-10"
    - from: "run_simplified_cv"
      to: "build_feature_model_pipeline"
      via: "pipeline construction"
      pattern: "build_feature_model_pipeline"
---

<objective>
Rewrite feature extraction to 29 dimensions and create simplified CV function.

Purpose: Reduce overfitting (139 dims on 43 subjects) by keeping only domain-relevant features, and simplify the classification pipeline.

Output: Modified modma_mdd_real_experiment.py with 29-dim extract_features and new run_simplified_cv.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-permutation-opt/03-CONTEXT.md
@.planning/phases/03-feature-permutation-opt/03-RESEARCH.md
@modma_mdd_real_experiment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite extract_features to 29 dims and add Theta/Beta ratio</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
Rewrite extract_features() (lines 314-383) to produce exactly 29 features:

1. PSD relative power (20 dims): Keep Welch PSD + relative band power per region. Remove region_std and PSD absolute from the region loop. Only append bp/(tp+1e-10) per band per region.

2. Alpha asymmetry frontal+temporal (2 dims): Generalize _alpha_asymmetry() to accept a region parameter (default "frontal"). Call twice: "frontal" and "temporal". Temporal uses EGI128_LEFT["temporal"] / EGI128_RIGHT["temporal"]. Fallback to zeros if channels missing.

3. Theta/Beta ratio per region (5 dims, FEAT-01): For each region compute mean(band_power["theta"][:,idx]) / (mean(band_power["beta"][:,idx]) + 1e-10). Feature name: {region}_theta_beta_ratio.

4. Riemannian top-2 (2 dims): Inline computation. Compute region-averaged signals for central, temporal, parietal only. Build 3x3 covariance, regularize +1e-6*I, log(abs()). Extract cov[0,1] for central_temporal and cov[0,2] for central_parietal. Names: riem_central_temporal, riem_central_parietal.

5. Remove calls to: compute_de_features, compute_hjorth_features, compute_lateral_asymmetry, compute_connectivity_features, compute_riemannian_features, old _alpha_asymmetry single-call.

6. Delete dead functions: compute_de_features, compute_hjorth_features, compute_lateral_asymmetry, compute_connectivity_features, _apply_qc_and_extract, run_full_model_selection, run_nested_group_cv, _run_one_permutation. Keep compute_riemannian_features (unused but harmless).

7. Simplify build_feature_model_pipeline: Remove PCA/SelectKBest branches and LightGBM branch. Keep scaler+SVM or scaler+Logistic only.

8. Simplify _get_model_candidates: Remove LightGBM. Return SVM and Logistic only.
  </action>
  <verify>python -c "import modma_mdd_real_experiment as m; print('Import OK')"</verify>
  <done>extract_features returns 29 features: 20 PSD-rel + 2 alpha-asym + 5 TBR + 2 riem. Dead functions deleted. Pipeline simplified.</done>
</task>

<task type="auto">
  <name>Task 2: Create run_simplified_cv for pre-extracted features</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
Create run_simplified_cv(features, y, groups, n_splits=5, seed=42):

1. Takes pre-extracted 29-dim feature matrix, labels, groups.
2. StratifiedGroupKFold outer CV.
3. Per fold: split features directly (no re-extraction). For each candidate from _get_model_candidates (SVM C=[0.1,1,10], Logistic C=[0.01,0.1,1]):
   - build_feature_model_pipeline(model_name, reducer="none")
   - If min_class_in_train >= 3: GridSearchCV with inner StratifiedGroupKFold, scoring="balanced_accuracy"
   - Else: fit with compute_subject_balanced_sample_weights, score=0.5
   - Pick best, retrain on full train fold, predict_proba on test fold.
4. Aggregate window predictions per subject (mean proba).
5. Return dict: {"primary_metric_level": "subject", "subject_level_metrics": {ba, roc_auc, f1}, "y_subj_true", "y_subj_prob", "subj_list", "fold_results"}.

No QC threshold loop. No _apply_qc_and_extract. No feature re-extraction.
  </action>
  <verify>python -c "import modma_mdd_real_experiment as m; assert hasattr(m, 'run_simplified_cv'); print('OK')"</verify>
  <done>run_simplified_cv exists, takes pre-extracted features, returns subject-level metrics. No re-extraction inside.</done>
</task>

</tasks>

<verification>
1. python -c "import modma_mdd_real_experiment as m; assert not hasattr(m, 'compute_de_features'); print('Dead code removed')"
2. python -c "import modma_mdd_real_experiment as m; assert hasattr(m, 'run_simplified_cv'); print('New CV exists')"
</verification>

<success_criteria>
- extract_features produces exactly 29 dimensions
- Theta/Beta ratio (FEAT-01) computed per region with epsilon guard
- run_simplified_cv works on pre-extracted features
- Dead feature functions removed, pipeline simplified to SVM+Logistic
- Module imports without errors
</success_criteria>

<output>
After completion, create .planning/phases/03-feature-permutation-opt/03-01-SUMMARY.md
</output>
