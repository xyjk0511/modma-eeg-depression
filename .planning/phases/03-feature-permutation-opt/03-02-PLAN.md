---
phase: 03-feature-permutation-opt
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - modma_mdd_real_experiment.py
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "Features extracted exactly once before permutation loop"
    - "Permutation loop only shuffles subject labels and calls run_simplified_cv"
    - "1000 permutations complete in under 50% of baseline wall-clock"
    - "run_main_with_output_dir uses simplified flow"
    - "BA >= 0.5 on real MODMA data"
  artifacts:
    - path: "modma_mdd_real_experiment.py"
      provides: "Rewritten build_report and run_main_with_output_dir"
      contains: "run_simplified_cv"
  key_links:
    - from: "build_report"
      to: "run_simplified_cv"
      via: "permutation loop calls"
      pattern: "run_simplified_cv.*features.*y_perm"
    - from: "run_main_with_output_dir"
      to: "extract_features"
      via: "single extraction call"
      pattern: "extract_features.*X_filtered"
---

<objective>
Rewrite permutation loop and main entry point to use pre-extracted features.

Purpose: Eliminate redundant feature extraction in permutation test (ARCH-02), reducing 1000-permutation wall-clock from ~3.5 hours to ~5 minutes.

Output: Modified build_report and run_main_with_output_dir using simplified flow.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-permutation-opt/03-CONTEXT.md
@.planning/phases/03-feature-permutation-opt/03-RESEARCH.md
@.planning/phases/03-feature-permutation-opt/03-01-SUMMARY.md
@modma_mdd_real_experiment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite build_report with pre-extracted feature permutation</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
Rewrite build_report() to accept pre-extracted features instead of X_raw:

New signature: build_report(features, y, groups, cv_results, n_permutations=1000, seed=42, n_jobs=4, n_splits=5)

Remove parameters: ch_names, sfreq, bad_amp_candidates, max_bad_channels, min_windows_per_subject.

Permutation loop changes:
1. Pre-generate permuted subject-level labels: rng.permutation(group_labels).
2. Map shuffled labels back to window-level: label_map = dict(zip(unique_groups, shuffled_labels)), y_perm = array([label_map[g] for g in groups]).
3. Skip permutations where len(unique(permuted_group_labels)) < 2.
4. Each permutation calls run_simplified_cv(features, y_perm, groups, n_splits, seed) and extracts BA.
5. Use Parallel(n_jobs=n_jobs)(delayed(...)) for parallelism.
6. p-value formula unchanged: (sum(perm_ba >= actual_ba) + 1) / (n_perm + 1).

Update report["permutation_strategy"] to "label_permutation_pre_extracted".
Update report["experiment_type"] to "feature_reduced_v5".

Keep: CI bootstrap, all metric fields, permutation_scores list.
  </action>
  <verify>python -c "import inspect; import modma_mdd_real_experiment as m; sig = inspect.signature(m.build_report); assert 'features' in sig.parameters; assert 'X_raw' not in sig.parameters; print('OK')"</verify>
  <done>build_report accepts pre-extracted features, permutation loop calls run_simplified_cv without re-extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite run_main_with_output_dir for simplified flow</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
Rewrite run_main_with_output_dir to use the new simplified flow:

1. Keep: load_windows, dynamic_max_bad, QC report generation, validation checks.
2. After QC filtering (X_filtered, y_filtered, groups_filtered):
   - Extract features ONCE: features, feature_names = extract_features(X_filtered, sfreq=resample_sfreq, ch_names=ch_names)
   - Compute n_splits from filtered data (same logic as before).
   - Call run_simplified_cv(features, y_filtered, groups_filtered, n_splits, seed).
   - Call build_report(features, y_filtered, groups_filtered, cv_results, n_permutations, seed, n_jobs, n_splits).
3. Remove: bad_amp_candidates tuple, passing X_windows/y_windows/groups to CV and report.
4. Keep: elapsed_seconds timing, model_comparison CSV, feature_importance CSV, permutation_scores CSV, ROC curve, confusion matrix, metrics.json, conclusion.
5. Feature importance: use the already-extracted features variable (no second extract_features call).
6. Remove the lenient_mask / separate QC filtering block — use fixed 200uV with dynamic_max_bad directly.

The flow becomes:
  load_windows -> QC mask (fixed 200uV) -> filter -> extract_features (ONCE) -> run_simplified_cv -> build_report -> save outputs
  </action>
  <verify>python -c "import modma_mdd_real_experiment as m; print('Import OK')"</verify>
  <done>run_main_with_output_dir uses simplified flow: single feature extraction, run_simplified_cv, pre-extracted permutation.</done>
</task>

</tasks>

<verification>
1. python -c "import inspect; import modma_mdd_real_experiment as m; sig = inspect.signature(m.build_report); assert 'features' in sig.parameters; print('build_report signature OK')"
2. python -c "import modma_mdd_real_experiment as m; print('Full import OK')"
3. Run on real data with 10 permutations to verify end-to-end: python modma_mdd_real_experiment.py --bids-root "d:/eeg/MODMA_EEG_BIDS_format/EEG_LZU_2015_2_resting state" --output-dir results_phase03_verify --n-permutations 10 --n-jobs 4
4. Check metrics.json: features count == 29, elapsed_seconds significantly less than baseline
</verification>

<success_criteria>
- Features extracted exactly once (not inside permutation loop) — ARCH-02
- 1000 permutations wall-clock reduced >= 50% vs baseline 2517s
- Pipeline runs end-to-end on real MODMA data
- metrics.json contains BA, p-value, CI, permutation_scores
- BA >= 0.5 (above chance with 29-dim features)
</success_criteria>

<output>
After completion, create .planning/phases/03-feature-permutation-opt/03-02-SUMMARY.md
</output>
