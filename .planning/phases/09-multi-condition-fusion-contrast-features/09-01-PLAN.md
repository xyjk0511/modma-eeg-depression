---
phase: 09-multi-condition-fusion-contrast-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - run_modma_erp.py
  - .planning/phases/09-multi-condition-fusion-contrast-features/09-01-SUMMARY.md
autonomous: true
requirements:
  - ERP-10
  - ERP-11
  - ERP-12

must_haves:
  truths:
    - "Three-condition fusion (hcue+fcue+scue) runs with explicit sub_id alignment and prints n_common"
    - "Contrast (scue-hcue) runs as ablation and prints shape"
    - "Both experiments report BA and permutation p-value"
    - "If n_common < 40, result is reported as ablation (not primary conclusion)"
  artifacts:
    - path: "run_modma_erp.py"
      provides: "Phase 9 block: fuse_conditions + contrast + permutation"
      contains: "fuse_conditions"
    - path: ".planning/phases/09-multi-condition-fusion-contrast-features/09-01-SUMMARY.md"
      provides: "Recorded BA, p-value, n_common for fusion and contrast"
  key_links:
    - from: "run_modma_erp.py Phase 9 block"
      to: "fuse_conditions()"
      via: "explicit sub_id intersection"
      pattern: "fuse_conditions"
    - from: "run_modma_erp.py Phase 9 block"
      to: "permutation_test_loso()"
      via: "1000-perm subject-level shuffle"
      pattern: "permutation_test_loso"
---

<objective>
Run the already-implemented Phase 9 block in run_modma_erp.py, record results, and write SUMMARY.

Purpose: Satisfy ERP-10/11/12 — multi-condition fusion and contrast ablation with permutation validation.
Output: Recorded BA, p-value, n_common for fusion (hcue+fcue+scue) and contrast (scue-hcue).
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 9 block and capture results</name>
  <files>run_modma_erp.py</files>
  <action>
The Phase 9 block (lines 202-220) is already implemented. Run the script and capture stdout.

The block:
1. Loads hcue/fcue/scue as dicts via load_erp_features_dict()
2. Calls fuse_conditions() — intersects sub_ids, hstacks features → X_fused (384-dim)
3. Prints "Fused matrix: (N, 384)  (n=N)" — record N as n_common
4. Runs run_loso() + permutation_test_loso(1000) on fused → record BA, p-value
5. Computes contrast X_contrast = scue − hcue for common subjects
6. Runs run_loso() + permutation_test_loso(1000) on contrast → record BA, p-value

Run command:
  cd D:/eeg && python run_modma_erp.py 2>&1 | tee /tmp/phase9_out.txt

Capture from stdout:
- n_common (from "Fused matrix: (N, 384)")
- fusion BA (from run_loso label="fusion(hcue+fcue+scue)")
- fusion p-value (from permutation_test_loso after fusion)
- contrast n (from "Contrast matrix: (N, 128)")
- contrast BA (from run_loso label="contrast(scue-hcue)")
- contrast p-value (from permutation_test_loso after contrast)

Note: STATE.md already records preliminary results (fusion BA=0.521 p=0.411, contrast BA=0.521 p=0.383).
If script output matches, use those values directly.
  </action>
  <verify>Script exits 0; stdout contains "fusion(hcue+fcue+scue)" and "contrast(scue-hcue)" lines with BA values.</verify>
  <done>n_common, fusion BA, fusion p-value, contrast BA, contrast p-value all recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write SUMMARY with results</name>
  <files>.planning/phases/09-multi-condition-fusion-contrast-features/09-01-SUMMARY.md</files>
  <action>
Write SUMMARY.md with the captured values. Use this template, substituting actual numbers:

---
phase: 09-multi-condition-fusion-contrast-features
plan: 01
status: complete
---

## Results

| Experiment | N | Dims | BA | p-value | Conclusion |
|------------|---|------|----|---------|------------|
| Fusion (hcue+fcue+scue) | {n_common} | 384 | {fusion_ba} | {fusion_p} | {pass/fail vs 0.70} |
| Contrast (scue-hcue) | {contrast_n} | 128 | {contrast_ba} | {contrast_p} | ablation |

## ERP-12 Assessment

n_common = {n_common}. If >= 40: primary result. If < 40: reported as ablation only.

## Conclusion

One sentence: whether fusion BA > 0.70 and p < 0.05 was achieved.
If n_common < 40: note that fusion result is ablation per ERP-12, and hcue single-condition
(BA=0.670, p=0.022) remains the primary finding.
  </action>
  <verify>File exists at .planning/phases/09-multi-condition-fusion-contrast-features/09-01-SUMMARY.md with all four numeric values filled in.</verify>
  <done>SUMMARY.md written with fusion BA, fusion p, contrast BA, contrast p, n_common, and ERP-12 assessment.</done>
</task>

</tasks>

<verification>
- python run_modma_erp.py exits 0
- stdout shows Phase 9 fusion and contrast sections with BA values
- 09-01-SUMMARY.md exists with all result fields populated
- ERP-12 assessment present (n_common vs 40 threshold)
</verification>

<success_criteria>
- ERP-10: fuse_conditions() ran with explicit sub_id alignment, n_common printed
- ERP-11: contrast (scue-hcue) ran and BA + p-value recorded
- ERP-12: n_common assessed against 40 threshold; result classified as primary or ablation accordingly
</success_criteria>

<output>
After completion, create .planning/phases/09-multi-condition-fusion-contrast-features/09-01-SUMMARY.md
</output>
