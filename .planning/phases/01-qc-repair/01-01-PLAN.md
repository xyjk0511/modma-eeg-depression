---
phase: 01-qc-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modma_mdd_real_experiment.py
autonomous: true
requirements:
  - QC-01
  - QC-02
  - QC-03
  - PRE-01

must_haves:
  truths:
    - "Highpass filter defaults to 1.0 Hz instead of 0.5 Hz"
    - "Bad channels are interpolated via MNE spherical spline before average reference"
    - "Window 0 is skipped for every subject"
    - "max_bad_channels is dynamically computed as 12% of channel count"
  artifacts:
    - path: "modma_mdd_real_experiment.py"
      provides: "Updated load_windows with montage, interpolation, W0 skip, reordered reref"
      contains: "interpolate_bads"
    - path: "modma_mdd_real_experiment.py"
      provides: "Dynamic max_bad_channels in run_main_with_output_dir"
      contains: "int(n_channels * 0.12)"
  key_links:
    - from: "load_windows()"
      to: "raw.interpolate_bads()"
      via: "set_montage -> detect bad -> mark bads -> interpolate -> reref"
      pattern: "set_montage.*interpolate_bads.*set_eeg_reference"
    - from: "run_main_with_output_dir()"
      to: "build_quality_mask()"
      via: "dynamic max_bad_channels passed instead of CLI fixed value"
      pattern: "int\(.*0\.12\)"
---

<objective>
Repair the core preprocessing pipeline in load_windows() and wire dynamic QC parameters through run_main_with_output_dir().

Purpose: Fix the QC bottleneck that drops 80% of subjects by implementing montage-based interpolation, skipping W0, raising highpass to 1.0 Hz, and using dynamic max_bad_channels.

Output: Updated modma_mdd_real_experiment.py with working interpolation pipeline.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qc-repair/01-CONTEXT.md
@.planning/phases/01-qc-repair/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parse_args and load_windows with interpolation pipeline</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
1. In `parse_args()` (line 101): Change `--highpass-freq` default from `0.5` to `1.0`.

2. In `load_windows()` (line 161): Add `bad_amp_uv=200.0` parameter to function signature.

3. Inside `load_windows()`, restructure preprocessing to this order:
   a. After `raw = mne.io.read_raw_edf(...)`, set montage:
      `montage = mne.channels.make_standard_montage("GSN-HydroCel-128")`
      `raw.set_montage(montage, verbose=False)`
   b. Crop (existing)
   c. Highpass filter (existing)
   d. Detect bad channels on Raw data:
      `data_raw = raw.get_data(); thr_v = bad_amp_uv * 1e-6`
      `bad_mask = np.any(np.abs(data_raw) > thr_v, axis=1)`
      `bad_chs = [raw.ch_names[i] for i in range(len(raw.ch_names)) if bad_mask[i]]`
   e. Interpolate if <=25% bad:
      `if 0 < len(bad_chs) <= int(len(raw.ch_names) * 0.25):`
      `    raw.info['bads'] = bad_chs`
      `    raw.interpolate_bads(reset_bads=True, verbose=False)`
   f. MOVE `set_eeg_reference('average')` to AFTER interpolation
   g. Resample (existing)

4. Change windowing loop from `range(0, data.shape[1] - window_size + 1, window_size)` to `range(window_size, data.shape[1] - window_size + 1, window_size)` to skip Window 0.

5. Track interpolation metadata: add `interp_info` dict mapping subject_id to n_interpolated. Return as 6th element of the tuple.

Per user locked decisions:
- Use amplitude threshold detection (NOT pyprep)
- Keep 200uV threshold unchanged
- Interpolate BEFORE average reference
  </action>
  <verify>
`python -c "from modma_mdd_real_experiment import parse_args; a = parse_args(['--bids-root', '.']); assert a.highpass_freq == 1.0"`
`python -c "import inspect; from modma_mdd_real_experiment import load_windows; assert 'bad_amp_uv' in inspect.signature(load_windows).parameters"`
Grep for `interpolate_bads` in load_windows function.
Grep for `range(window_size,` confirming W0 skip.
Verify `set_eeg_reference` appears AFTER `interpolate_bads`.
  </verify>
  <done>
- --highpass-freq defaults to 1.0
- load_windows sets GSN-HydroCel-128 montage, detects bad channels, interpolates (<=25% bad), then rerefs
- Window 0 is skipped
- Interpolation metadata returned
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire dynamic max_bad_channels through run_main_with_output_dir</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
1. In `run_main_with_output_dir()`: after `load_windows()` returns, compute:
   `n_channels = X_windows.shape[1]`
   `dynamic_max_bad = int(n_channels * 0.12)`
   Log: `logger.info(f"Dynamic max_bad_channels: {dynamic_max_bad} (12% of {n_channels})")`

2. Use `dynamic_max_bad` for all downstream calls replacing `max_bad_channels`:
   - `build_quality_mask(..., max_bad_channels=dynamic_max_bad)`
   - `run_full_model_selection(..., max_bad_channels=dynamic_max_bad)`
   - `build_report(..., max_bad_channels=dynamic_max_bad)`

3. Pass `bad_amp_uv=bad_amp_uv` to the `load_windows()` call.

4. Keep CLI `--max-bad-channels` arg for backward compat but override with dynamic value.
  </action>
  <verify>
`python -c "from modma_mdd_real_experiment import load_windows; print('import ok')"`
Grep for `int(n_channels * 0.12)` in run_main_with_output_dir.
Grep for `dynamic_max_bad` usage.
  </verify>
  <done>
- max_bad_channels computed as int(n_channels * 0.12) = 15 for 128ch
- Dynamic value used in build_quality_mask, run_full_model_selection, build_report
  </done>
</task>

</tasks>

<verification>
1. `python -c "from modma_mdd_real_experiment import parse_args; a = parse_args(['--bids-root', '.']); assert a.highpass_freq == 1.0"` passes
2. `grep -n "interpolate_bads" modma_mdd_real_experiment.py` shows interpolation in load_windows
3. `grep -n "set_eeg_reference" modma_mdd_real_experiment.py` shows reref AFTER interpolate_bads
4. `grep -n "range(window_size," modma_mdd_real_experiment.py` shows W0 skip
5. `grep -n "0.12" modma_mdd_real_experiment.py` shows dynamic max_bad_channels
</verification>

<success_criteria>
- Highpass defaults to 1.0 Hz (PRE-01)
- Bad channels interpolated via spherical spline before average reference (QC-02)
- Window 0 skipped for all subjects (QC-03)
- max_bad_channels dynamically computed as 12% of channel count (QC-01)
- Pipeline compiles without import errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-qc-repair/01-01-SUMMARY.md`
</output>
