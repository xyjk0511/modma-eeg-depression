---
phase: 01-qc-repair
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - modma_mdd_real_experiment.py
autonomous: true
requirements:
  - QC-04
  - VAL-03

must_haves:
  truths:
    - "QC report includes per-subject interpolation count and channel names"
    - "QC report shows MDD and HC group retention rates and their difference"
    - "Pipeline retains >= 35 subjects after all QC changes"
  artifacts:
    - path: "modma_mdd_real_experiment.py"
      provides: "Extended generate_qc_report with interpolation and group retention"
      contains: "group_retention"
  key_links:
    - from: "run_main_with_output_dir()"
      to: "generate_qc_report()"
      via: "interp_info dict passed from load_windows"
      pattern: "interp_info"
    - from: "generate_qc_report()"
      to: "qc_report.csv"
      via: "DataFrame with n_interpolated and group retention columns"
      pattern: "n_interpolated"
---

<objective>
Extend QC report with interpolation metadata and group retention rates, then verify the full pipeline retains >= 35 subjects.

Purpose: Satisfy VAL-03 (MDD/HC balance reporting) and QC-04 (>= 35 subjects retained). Depends on Plan 01's interpolation pipeline.

Output: Updated generate_qc_report() and verified pipeline retention.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-qc-repair/01-CONTEXT.md
@.planning/phases/01-qc-repair/01-RESEARCH.md
@.planning/phases/01-qc-repair/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generate_qc_report with interpolation and group retention</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
1. Update `generate_qc_report()` signature to accept `interp_info=None` (dict of subject_id -> n_interpolated from load_windows).

2. For each subject row, add `n_interpolated` field from interp_info (0 if not in dict).

3. After building all rows, compute group retention summary:
   - Count retained MDD subjects (drop_reason == "kept" and label == "MDD")
   - Count retained HC subjects (drop_reason == "kept" and label == "HC")
   - Compute retention rates: `mdd_rate = n_mdd_kept / n_mdd_total`, `hc_rate = n_hc_kept / n_hc_total`
   - Compute balance diff: `abs(mdd_rate - hc_rate)`
   - Log these values: `logger.info(f"Group retention - MDD: {mdd_rate:.0%}, HC: {hc_rate:.0%}, diff: {balance_diff:.0%}")`

4. Have `generate_qc_report` return `(df, retention_stats_dict)` where retention_stats_dict has keys `mdd_rate`, `hc_rate`, `diff`. Update `run_main_with_output_dir` to save retention_stats into metrics.json.

5. In `run_main_with_output_dir()`: unpack interp_info from `load_windows()` 6th return value:
   `X_windows, y_labels, subject_ids, window_ids, ch_names, interp_info = load_windows(...)`
   Pass `interp_info=interp_info` to `generate_qc_report()`. Save retention stats to metrics.json report dict.
  </action>
  <verify>
`python -c "import inspect; from modma_mdd_real_experiment import generate_qc_report; assert 'interp_info' in inspect.signature(generate_qc_report).parameters"`
Grep for `group_retention` in modma_mdd_real_experiment.py.
Grep for `n_interpolated` in generate_qc_report function.
  </verify>
  <done>
- generate_qc_report accepts interp_info and includes n_interpolated per subject
- Group retention rates (MDD, HC, diff) computed and logged
- Retention stats saved to metrics.json
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end pipeline verification on real data</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
Run the full pipeline on real MODMA data to verify retention >= 35 subjects:

```bash
python modma_mdd_real_experiment.py \
  --bids-root "d:/eeg/MODMA_EEG_BIDS_format/EEG_LZU_2015_2_resting state" \
  --output-dir results_phase01_verify \
  --n-permutations 10 \
  --n-jobs 2
```

Check results:
1. Parse qc_report.csv: count rows where drop_reason == "kept" — must be >= 35
2. Check metrics.json for group_retention_diff < 0.20
3. Verify no import errors or runtime crashes

If retention < 35, investigate qc_report.csv to identify which subjects are being dropped and why. The research predicts 43 subjects retained — if significantly fewer, check that interpolation is working correctly.

NOTE: This task verifies QC-04 (>= 35 subjects) empirically. If the pipeline run takes too long with 1000 permutations, use --n-permutations 10 for a quick verification run.
  </action>
  <verify>
`python -c "import pandas as pd; df = pd.read_csv('results_phase01_verify/qc_report.csv'); kept = df[df['drop_reason']=='kept']; print(f'Retained: {len(kept)} subjects'); assert len(kept) >= 35"`
`python -c "import json; r = json.load(open('results_phase01_verify/metrics.json')); print(r.get('group_retention', {}))"`
  </verify>
  <done>
- Pipeline runs end-to-end without errors
- >= 35 subjects retained (QC-04)
- MDD/HC retention diff < 20% (VAL-03)
- Results saved to results_phase01_verify/
  </done>
</task>

</tasks>

<verification>
1. qc_report.csv has n_interpolated column with per-subject values
2. Pipeline retains >= 35 subjects (research predicts 43)
3. MDD/HC group retention difference < 20% (research predicts 11%)
4. metrics.json contains group retention stats
</verification>

<success_criteria>
- QC report shows interpolation info per subject (VAL-03)
- QC report shows group retention rates with diff < 20% (VAL-03)
- Pipeline retains >= 35 subjects on real MODMA data (QC-04)
- No runtime errors on full dataset
</success_criteria>

<output>
After completion, create `.planning/phases/01-qc-repair/01-02-SUMMARY.md`
</output>
