---
phase: 06-literature-informed-feature-expansion
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [run_phase6_replication.py, external_data_adapter.py]
autonomous: true
requirements: [REP-01, VAL-01]

must_haves:
  truths:
    - "Model trained on full MODMA 15-dim, tested on TDBRAIN 15-dim"
    - "No re-tuning on TDBRAIN data"
    - "Cross-dataset permutation test produces p-value"
    - "Feature distribution comparison MODMA vs TDBRAIN generated"
    - "Complete report with BA, CI, p-value, conclusion"
  artifacts:
    - path: "run_phase6_replication.py"
      provides: "Cross-dataset replication with 15-dim features"
      contains: "cross_dataset_replication"
    - path: "external_data_adapter.py"
      provides: "Updated self-test for 15 features"
      contains: "assert.*15"
  key_links:
    - from: "run_phase6_replication.py"
      to: "modma_mdd_real_experiment.py:extract_features"
      via: "import for 15-dim feature extraction"
      pattern: "extract_features"
    - from: "run_phase6_replication.py"
      to: "external_data_adapter.py:load_tdbrain_features"
      via: "import for TDBRAIN loading"
      pattern: "load_tdbrain_features"
---

<objective>
Cross-dataset validation: train 15-dim model on MODMA, test on TDBRAIN, compare with Phase 5 baseline.

Purpose: Determine if literature-informed 15-dim features generalize better than the 5-dim set that failed (BA=0.500).
Output: run_phase6_replication.py, complete report with BA, CI, p-value, feature distribution analysis.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-literature-informed-feature-expansion/06-01-SUMMARY.md
@modma_mdd_real_experiment.py
@external_data_adapter.py
@run_phase5_replication.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-dataset replication with 15-dim features</name>
  <files>run_phase6_replication.py, external_data_adapter.py</files>
  <action>
Update external_data_adapter.py self-test: change assertion from 5 to 15 features.

Create run_phase6_replication.py based on run_phase5_replication.py structure:
1. Load MODMA (load_participants + load_windows), QC, extract 15-dim features
2. Train on full MODMA with build_feature_model_pipeline(use_l1=True)
3. Load TDBRAIN via load_tdbrain_features (auto-gets 15 features)
4. Predict on TDBRAIN, aggregate to subject-level BA
5. Cross-dataset permutation test (1000 perms, permute subject labels)
6. Feature distribution analysis: for each of 15 features, compute mean/std per dataset, save to report
7. Save results_phase6_replication/metrics.json with: ba, ci, p_value, feature_names, feature_distributions, comparison with Phase 5 baseline (ba_phase5=0.500)
8. Save ROC curve and confusion matrix PNGs

Args: --bids-root, --tdbrain-root, --output-dir (default results_phase6_replication), --n-permutations (1000), --n-jobs (4).
  </action>
  <verify>
python run_phase6_replication.py --bids-root D:/eeg/ds003478 --tdbrain-root D:/eeg/tdbrain --n-permutations 10 --output-dir results_phase6_rep_test
Check: results_phase6_rep_test/metrics.json has ba, ci, p_value, feature_names (15), feature_distributions.
  </verify>
  <done>Complete cross-dataset report with BA, CI, p-value, feature distributions, Phase 5 comparison</done>
</task>

</tasks>

<verification>
1. external_data_adapter.py self-test passes with 15 features
2. run_phase6_replication.py produces metrics.json with all required fields
3. Feature distribution analysis shows per-feature mean/std for both datasets
</verification>

<success_criteria>
- TDBRAIN test with 15-dim features produces BA, CI, p-value report
- Feature distribution comparison MODMA vs TDBRAIN saved
- Complete conclusion (positive or negative) documented
</success_criteria>

<output>
After completion, create .planning/phases/06-literature-informed-feature-expansion/06-02-SUMMARY.md
</output>
