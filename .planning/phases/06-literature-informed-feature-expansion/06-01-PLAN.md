---
phase: 06-literature-informed-feature-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [modma_mdd_real_experiment.py, run_phase6_validation.py]
autonomous: true
requirements: [FEAT-02, VAL-01]

must_haves:
  truths:
    - "extract_features() returns exactly 15 features per window"
    - "Existing 5 features unchanged (backward compatible)"
    - "PLV via Hilbert on bandpass-filtered region-averaged signals"
    - "Coherence via scipy.signal.coherence on region-averaged signals"
    - "MODMA internal CV produces 5-dim vs 15-dim BA comparison"
    - "15-dim model uses L1 penalty with saga solver"
  artifacts:
    - path: "modma_mdd_real_experiment.py"
      provides: "15-dim extract_features"
      contains: "plv_frontal_parietal_alpha"
    - path: "run_phase6_validation.py"
      provides: "Internal MODMA 5-dim vs 15-dim comparison"
      contains: "balanced_accuracy"
  key_links:
    - from: "extract_features"
      to: "_alpha_asymmetry"
      via: "temporal and beta asymmetry calls"
      pattern: "_alpha_asymmetry.*temporal"
    - from: "extract_features"
      to: "scipy.signal.hilbert"
      via: "PLV computation"
      pattern: "hilbert"
    - from: "extract_features"
      to: "scipy.signal.coherence"
      via: "coherence computation"
      pattern: "coherence"
---

<objective>
Expand extract_features() from 5 to 15 dimensions based on MDD literature, validate internally on MODMA.

Purpose: Phase 5 replication failed (BA=0.500). Literature identifies beta power, PLV connectivity, and temporal/central regions as more discriminative for MDD.
Output: 15-dim extract_features(), run_phase6_validation.py with 5-dim vs 15-dim comparison.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@modma_mdd_real_experiment.py
@external_data_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand extract_features from 5 to 15 dimensions</name>
  <files>modma_mdd_real_experiment.py</files>
  <action>
Modify extract_features() at line 304. Keep existing 5 features unchanged.

Add import at top: from scipy.signal import hilbert as sig_hilbert, sosfilt, butter, coherence as sig_coherence

Add 10 new features after existing 5:

A. Beta power (4 features) — reuse existing _band_rel inner function:
- temporal_beta2_rel: _band_rel(tidx, 16, 24) where tidx = region_idx["temporal"]
- frontal_beta2_rel: _band_rel(fidx, 16, 24) where fidx = region_idx["frontal"]
- temporal_beta3_rel: _band_rel(tidx, 24, 40)
- beta_asym_temporal: compute beta2 power (16-24Hz mask on psd), call _alpha_asymmetry(beta2_power, ch_names, n_win, "temporal")

B. Connectivity (3 features) — add two helpers OUTSIDE extract_features:

_compute_plv(X, region_idx, sfreq, region_a, region_b, fmin, fmax):
  sig_a = mean of X[:, region_idx[region_a], :] over channels (axis=1)
  sig_b = mean of X[:, region_idx[region_b], :] over channels (axis=1)
  sos = butter(4, [fmin, fmax], btype='band', fs=sfreq, output='sos')
  fa = sosfilt(sos, sig_a, axis=1); fb = sosfilt(sos, sig_b, axis=1)
  pa = np.angle(sig_hilbert(fa, axis=1)); pb = np.angle(sig_hilbert(fb, axis=1))
  plv = np.abs(np.mean(np.exp(1j * (pa - pb)), axis=1))
  return plv[:, np.newaxis]

_compute_coherence(X, region_idx, sfreq, region_a, region_b, fmin, fmax):
  sig_a/sig_b same averaging as PLV
  nperseg = min(X.shape[2], int(sfreq * 2))
  Loop per window: freqs_c, cxy = sig_coherence(sig_a[i], sig_b[i], fs=sfreq, nperseg=nperseg)
  band_mask = (freqs_c >= fmin) & (freqs_c <= fmax)
  result[i] = mean(cxy[band_mask])
  return result[:, np.newaxis]

Call: plv_fp_alpha = _compute_plv(X, region_idx, sfreq, "frontal", "parietal", 8, 13)
     plv_ft_theta = _compute_plv(X, region_idx, sfreq, "frontal", "temporal", 4, 8)
     coh_ct_beta = _compute_coherence(X, region_idx, sfreq, "central", "temporal", 13, 30)

C. Temporal/central (3 features):
- temporal_theta_rel: _band_rel(tidx, 4, 8)
- temporal_alpha_rel: _band_rel(tidx, 8, 13)
- alpha_asym_temporal: _alpha_asymmetry(alpha_power, ch_names, n_win, "temporal")

Update names list to 15 entries. Update docstring.
  </action>
  <verify>
python -c "
import numpy as np
from modma_mdd_real_experiment import extract_features, STANDARD_1020_REGION_MAP
ch = list(STANDARD_1020_REGION_MAP.keys())
X = np.random.randn(4, len(ch), 1250) * 1e-5
f, n = extract_features(X, 125.0, ch)
assert f.shape == (4, 15), f'Shape {f.shape}'
assert len(n) == 15, f'Names {len(n)}'
assert not np.any(np.isnan(f)), 'NaN found'
print('OK:', n)
"
  </verify>
  <done>extract_features returns (N, 15) with 15 named features, no NaN, backward-compatible</done>
</task>

<task type="auto">
  <name>Task 2: MODMA internal validation comparing 5-dim vs 15-dim</name>
  <files>run_phase6_validation.py, modma_mdd_real_experiment.py</files>
  <action>
Update build_feature_model_pipeline in modma_mdd_real_experiment.py: add optional use_l1=False param. When True, use penalty='l1', solver='saga'. Default stays l2 for backward compat.

Create run_phase6_validation.py (model on run_phase5_replication.py):
1. Load MODMA via load_participants + load_windows
2. QC: 200uV, 12% dynamic bad, skip W0, min 3 windows/subject
3. Extract 15-dim features
4. Two CV runs (StratifiedGroupKFold n_splits=5, subject-level BA):
   a. 5-dim: first 5 columns, build_feature_model_pipeline(use_l1=False)
   b. 15-dim: all columns, build_feature_model_pipeline(use_l1=True)
5. 1000-perm test on 15-dim (permute labels, reuse run_simplified_cv pattern)
6. Save results_phase6/metrics.json: ba_5dim, ba_15dim, p_value, feature_names

Args: --bids-root, --output-dir (default results_phase6), --n-permutations (1000), --n-jobs (4).
  </action>
  <verify>
python run_phase6_validation.py --bids-root D:/eeg/ds003478 --n-permutations 10 --output-dir results_phase6_test
Check: results_phase6_test/metrics.json has ba_5dim, ba_15dim, p_value, feature_names (15 entries).
  </verify>
  <done>metrics.json shows 5-dim vs 15-dim BA comparison with permutation p-value</done>
</task>

</tasks>

<verification>
1. extract_features returns shape (N, 15) with no NaN for both EGI-128 and 10-20 channels
2. external_data_adapter.py self-test assertion updated from 5 to 15
3. run_phase6_validation.py produces metrics.json with ba_5dim, ba_15dim, p_value
</verification>

<success_criteria>
- extract_features() returns 15 features (5 existing + 4 beta + 3 connectivity + 3 temporal/central)
- MODMA internal CV comparison saved with both 5-dim and 15-dim BA values
- No regression: existing 5 features produce same values as before expansion
</success_criteria>

<output>
After completion, create .planning/phases/06-literature-informed-feature-expansion/06-01-SUMMARY.md
</output>
