---
phase: 08-permutation-test-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ERP-07, ERP-08, ERP-09, ERP-NF-01, ERP-NF-02]

must_haves:
  truths:
    - "permutation_test_loso() asserts len(y)==n_subjects before running"
    - "1000 permutations complete with joblib.Parallel n_jobs=4 seed=42"
    - "hcue p-value printed to stdout"
  artifacts:
    - path: "run_modma_erp.py"
      provides: "permutation_test_loso() already implemented"
      contains: "assert len(y) == X.shape[0]"
    - path: ".planning/phases/08-permutation-test-validation/08-01-SUMMARY.md"
      provides: "Observed BA and p-value recorded"
  key_links:
    - from: "__main__"
      to: "permutation_test_loso(X, y, n_perm=1000)"
      via: "direct call after run_loso()"
      pattern: "permutation_test_loso\(X, y"
---

<objective>
Run the already-implemented permutation test on hcue 128-dim P300 features and record the p-value.

Purpose: Confirm hcue BA=0.670 is statistically significant (p<0.05), satisfying ERP-07/08/09.
Output: Printed p-value + SUMMARY.md with result.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run hcue permutation test</name>
  <files></files>
  <action>
    The implementation is complete — no code changes needed. Run only the hcue block:

      cd D:/eeg && python -c "
import run_modma_erp as m
m.CONDITION = 'hcue'
X, y, ids = m.load_erp_features()
print(f'Feature matrix: {X.shape}')
m.run_loso(X, y, ids)
m.permutation_test_loso(X, y, n_perm=1000)
" 2>&1

    This avoids the slow multi-condition fusion in __main__.
    Record the line: "Observed BA=X.XXX  p=X.XXXX  (perm mean=X.XXX)"
    Do NOT modify run_modma_erp.py.
  </action>
  <verify>stdout contains "p=" followed by a 4-decimal float</verify>
  <done>p-value printed; value recorded for SUMMARY.md</done>
</task>

<task type="auto">
  <name>Task 2: Write SUMMARY.md with result</name>
  <files>.planning/phases/08-permutation-test-validation/08-01-SUMMARY.md</files>
  <action>
    Create SUMMARY.md with actual numbers from Task 1 output.

    # Phase 08-01 Summary: Permutation Test Validation

    ## Result

    | Metric | Value |
    |--------|-------|
    | Condition | hcue |
    | Features | 128-dim P300 mean amplitude |
    | N subjects | {n from "Total: N subjects"} |
    | Observed BA | {from "BA  = X.XXX"} |
    | Permutations | 1000 |
    | p-value | {from "p=X.XXXX"} |
    | Conclusion | significant / not significant |

    ## Interpretation

    {one sentence on whether p<0.05 target is met}

    ## Artifacts

    - run_modma_erp.py — no changes (permutation_test_loso already implemented)
  </action>
  <verify>File exists with actual numeric values (not placeholders)</verify>
  <done>SUMMARY.md contains observed BA, p-value, and conclusion from actual run</done>
</task>

</tasks>

<verification>
- stdout shows "Observed BA=0.670  p=X.XXXX  (perm mean=...)"
- assert len(y)==X.shape[0] did not raise (confirms subject-level permutation)
- SUMMARY.md exists with real values
</verification>

<success_criteria>
- hcue permutation test completes 1000 iterations without error
- p-value is printed and recorded in SUMMARY.md
- If p<0.05: ERP-07, ERP-08, ERP-09, ERP-NF-01, ERP-NF-02 satisfied
- If p>=0.05: record as negative result, proceed to Phase 9 regardless
</success_criteria>

<output>
After completion, create `.planning/phases/08-permutation-test-validation/08-01-SUMMARY.md`
</output>
