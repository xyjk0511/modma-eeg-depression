---
phase: 15-grand-average-erp-plot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/modma/run_modma_erp.py
  - outputs/out_phase15/grand_avg_erp.png
  - outputs/out_phase15/grand_avg_erp.pdf
autonomous: true
requirements: [ERP-04]

must_haves:
  truths:
    - "Grand-average ERP plot shows MDD (red solid) and HC (blue dashed) waveforms with SEM shading on Fz, Cz, Pz"
    - "250-500ms P300 window is shaded gray with P300 label; 0ms stimulus onset marked with vertical dashed line"
    - "Plot saved as both PNG (300 DPI) and PDF in outputs/out_phase15/"
  artifacts:
    - path: "scripts/modma/run_modma_erp.py"
      provides: "plot_grand_average_erp() function"
      contains: "def plot_grand_average_erp"
    - path: "outputs/out_phase15/grand_avg_erp.png"
      provides: "Raster ERP figure"
    - path: "outputs/out_phase15/grand_avg_erp.pdf"
      provides: "Vector ERP figure"
  key_links:
    - from: "plot_grand_average_erp"
      to: "load_erp_timeseries"
      via: "function call with hcue condition"
      pattern: "load_erp_timeseries.*hcue"
    - from: "plot_grand_average_erp"
      to: "get_parietal_indices"
      via: "channel index lookup for Fz, Cz, Pz"
      pattern: "get_parietal_indices.*Fz.*Cz.*Pz"
---

<objective>
Add `plot_grand_average_erp()` to run_modma_erp.py and execute it to produce the grand-average ERP waveform figure (ERP-04).

Purpose: Visually verify that the P300 window (250-500ms) shows a positive deflection in the hcue condition, closing the ERP-04 requirement gap.
Output: `outputs/out_phase15/grand_avg_erp.png` (300 DPI) and `outputs/out_phase15/grand_avg_erp.pdf` (vector).
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-grand-average-erp-plot/15-CONTEXT.md
@.planning/phases/15-grand-average-erp-plot/15-RESEARCH.md
@scripts/modma/run_modma_erp.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement plot_grand_average_erp and generate figures</name>
  <files>scripts/modma/run_modma_erp.py, outputs/out_phase15/grand_avg_erp.png, outputs/out_phase15/grand_avg_erp.pdf</files>
  <action>
Add `plot_grand_average_erp(out_dir)` function to `run_modma_erp.py`.

Implementation:
1. Call `load_erp_timeseries("hcue")` to get `(_, avg_erps, times, y, _)`.
2. Use `get_parietal_indices(("Fz", "Cz", "Pz"))` for channel index lookup -- do NOT hardcode indices.
3. Convert times to ms (`times * 1000`), amplitudes to uV (`* 1e6`).
4. Create `fig, axes = plt.subplots(3, 1, sharex=True, sharey=True)` -- three rows: Fz, Cz, Pz top-to-bottom.
5. For each channel subplot:
   - MDD group: red solid line (lw=1.5), +/-1 SEM shading (alpha=0.2). Label: `MDD (n=XX)`.
   - HC group: blue dashed line (lw=1.5), +/-1 SEM shading (alpha=0.2). Label: `HC (n=XX)`.
   - Use `scipy.stats.sem(arr, axis=0)` for SEM computation.
   - `ax.axvspan(250, 500, color="gray", alpha=0.15)` for P300 window.
   - `ax.text(375, 0.85, "P300", ha="center", fontsize=9, color="gray", transform=ax.get_xaxis_transform())` for label.
   - `ax.axvline(0, color="black", lw=0.8, ls="--")` for stimulus onset.
   - Y-axis label: channel name + "Amplitude (uV)".
6. Legend only in top subplot (Fz), `loc="upper right"`.
7. Bottom subplot x-label: "Time (ms)".
8. `fig.suptitle("Grand-Average ERP: hcue (MDD vs HC)")`.
9. Save PNG (dpi=300) and PDF, both with `bbox_inches="tight"`.
10. Use `matplotlib.use("Agg")` before pyplot import (headless).
11. Add `__main__` call: `plot_grand_average_erp(ROOT / "outputs" / "out_phase15")`.

Critical constraints:
- Y-axis positive UP (standard Cartesian, NOT negative-up). Do NOT invert y-axis.
- Plot full available epoch range (-100ms to 500ms). Do NOT change TMIN/TMAX.
- Do NOT add individual subject waveforms or topomaps.
  </action>
  <verify>
Run `python scripts/modma/run_modma_erp.py` with __main__ block that calls plot_grand_average_erp.
Confirm:
- `outputs/out_phase15/grand_avg_erp.png` exists and is > 50KB
- `outputs/out_phase15/grand_avg_erp.pdf` exists and is > 10KB
- No errors during execution
  </verify>
  <done>
- plot_grand_average_erp() added to run_modma_erp.py
- PNG (300 DPI) and PDF saved to outputs/out_phase15/
- Plot shows 3 subplots (Fz/Cz/Pz) with MDD red solid + HC blue dashed + SEM shading
- P300 window (250-500ms) shaded gray with label; stimulus onset at 0ms marked
  </done>
</task>

</tasks>

<verification>
1. `outputs/out_phase15/grand_avg_erp.png` exists, filesize > 50KB
2. `outputs/out_phase15/grand_avg_erp.pdf` exists, filesize > 10KB
3. Script runs without error
4. Visual inspection: P300 window (250-500ms) shows positive deflection in Pz subplot
</verification>

<success_criteria>
- ERP-04 satisfied: grand-average ERP plot covers full epoch range with MDD/HC grouping
- 250-500ms P300 window visible with gray shading and label
- Both PNG and PDF outputs saved to outputs/out_phase15/
</success_criteria>

<output>
After completion, create `.planning/phases/15-grand-average-erp-plot/15-01-SUMMARY.md`
</output>
