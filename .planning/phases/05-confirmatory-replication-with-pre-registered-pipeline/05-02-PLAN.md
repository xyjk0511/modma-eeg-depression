---
phase: 05-confirmatory-replication-with-pre-registered-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - run_phase5_replication.py
autonomous: false
requirements: [REP-03, REP-04]

must_haves:
  truths:
    - "Model is trained on full MODMA (43 subjects) and tested on external dataset with NO re-tuning"
    - "Permutation test permutes subject-level labels on external test set only (1000 iterations)"
    - "Complete report saved with BA, CI, p-value, and positive/negative conclusion"
  artifacts:
    - path: "run_phase5_replication.py"
      provides: "Main entry: train MODMA, test external, permutation test, save report"
      exports: ["main"]
    - path: "results_phase5/metrics.json"
      provides: "Full metrics with conclusion object"
      contains: "conclusion"
  key_links:
    - from: "run_phase5_replication.py"
      to: "modma_mdd_real_experiment.py"
      via: "imports build_feature_model_pipeline, extract_features, compute_subject_balanced_sample_weights, get_ci_bootstrap, determine_conclusion"
      pattern: "from modma_mdd_real_experiment import"
    - from: "run_phase5_replication.py"
      to: "external_data_adapter.py"
      via: "imports load_tdbrain_features"
      pattern: "from external_data_adapter import"
---

<objective>
Create the replication runner that trains on MODMA and tests on external data, with cross-dataset permutation test and full reporting.

Purpose: Execute the confirmatory replication and produce a definitive result (positive or negative).
Output: run_phase5_replication.py, results_phase5/metrics.json with conclusion
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-CONTEXT.md
@.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-RESEARCH.md
@.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-01-SUMMARY.md
@modma_mdd_real_experiment.py
@external_data_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replication runner script</name>
  <files>run_phase5_replication.py</files>
  <action>
Create `run_phase5_replication.py` with:

1. **MODMA training:** Load full MODMA dataset using existing `load_windows()` + `extract_features()`. Apply same QC (200uV, dynamic 12% bad, skip W0). Fit `build_feature_model_pipeline()` on ALL MODMA data (no CV split). Use `compute_subject_balanced_sample_weights()` for sample_weight.

2. **External testing:** Call `load_tdbrain_features()` from external_data_adapter.py. Run `pipe.predict_proba(ext_features)` using MODMA-fitted pipeline. Aggregate window predictions to subject-level (mean prob). Compute subject-level BA, ROC-AUC, F1.

3. **Bootstrap CI:** Use `get_ci_bootstrap()` on subject-level predictions.

4. **Cross-dataset permutation test (1000 iterations):**
   - Get unique subjects and labels from external test set
   - Per permutation: shuffle subject labels, propagate to windows, compute subject-level BA
   - p = (count(perm_ba >= actual_ba) + 1) / (n_perms + 1)
   - Use joblib Parallel

5. **Conclusion:** Call `determine_conclusion()` with report dict.

6. **Save:** metrics.json, ROC curve PNG, confusion matrix PNG to output dir.

7. **CLI args:** --bids-root, --tdbrain-root, --output-dir (default results_phase5), --n-permutations (1000), --n-jobs (4), --seed (42).

8. **Fallback:** If --tdbrain-root missing or load fails, print error with fallback instructions. Do NOT implement fallback.

CRITICAL: pipe.fit() on MODMA only. Never fit() on external data.
  </action>
  <verify>
    python -c "import run_phase5_replication; print('imports OK')"
    python run_phase5_replication.py --help
  </verify>
  <done>run_phase5_replication.py imports OK, --help shows all args, ready for TDBRAIN data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify replication results</name>
  <files>results_phase5/metrics.json</files>
  <action>
Human downloads TDBRAIN data and runs the replication script. Claude verifies output.
  </action>
  <verify>
1. git tag -l "pre-registration-v1" returns tag
2. Run: python run_phase5_replication.py --bids-root "d:/eeg/MODMA_EEG_BIDS_format/EEG_LZU_2015_2_resting state" --tdbrain-root PATH --output-dir results_phase5 --n-permutations 1000 --n-jobs 4
3. Check results_phase5/metrics.json has: balanced_accuracy, balanced_accuracy_ci95, permutation_pvalue, conclusion
  </verify>
  <done>metrics.json contains complete report with conclusion (positive or negative).</done>
</task>

</tasks>

<verification>
- run_phase5_replication.py exists and imports without error
- --help shows correct CLI arguments
- When run with real data: metrics.json contains BA, CI, p-value, conclusion
- Pipeline fit on MODMA only
- Permutation at subject level, not window level
</verification>

<success_criteria>
Replication script runs end-to-end. Results saved to metrics.json with definitive conclusion.
</success_criteria>

<output>
After completion, create `.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-02-SUMMARY.md`
</output>
