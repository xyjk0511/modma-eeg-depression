---
phase: 05-confirmatory-replication-with-pre-registered-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PRE_REGISTRATION.md
  - external_data_adapter.py
autonomous: true
requirements: [REP-01, REP-02]

must_haves:
  truths:
    - "Pre-registration document exists with all locked parameters before any external data is processed"
    - "Data adapter loads TDBRAIN BDF files, picks 10-20 channels, applies locked preprocessing, returns (features, y, groups)"
    - "Adapter reuses extract_features() and build_region_indices() from existing pipeline — no re-implementation"
    - "Resampling to 125Hz includes a spectrum sanity check confirming alpha peak near 10Hz and no Nyquist artifacts"
  artifacts:
    - path: "docs/PRE_REGISTRATION.md"
      provides: "Complete pre-registration with locked pipeline, preprocessing, evaluation criteria"
      contains: "LogisticRegression(C=0.1"
    - path: "external_data_adapter.py"
      provides: "TDBRAIN BDF loader with channel mapping, QC, and post-resample spectrum check"
      exports: ["load_tdbrain_subjects", "load_tdbrain_features"]
  key_links:
    - from: "external_data_adapter.py"
      to: "modma_mdd_real_experiment.py"
      via: "imports extract_features, build_region_indices, STANDARD_1020_REGION_MAP"
      pattern: "from modma_mdd_real_experiment import"
---

<objective>
Create the pre-registration document and external data adapter for TDBRAIN, then git-tag the pre-registration commit BEFORE any external data touches the classifier.

Purpose: Establish scientific rigor — the pipeline is locked and documented before confirmatory testing.
Output: PRE_REGISTRATION.md (git-tagged), external_data_adapter.py (tested on dummy data only)
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-CONTEXT.md
@.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-RESEARCH.md
@.planning/phases/04-statistical-validation/04-VERIFICATION.md
@modma_mdd_real_experiment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-registration document + external data adapter</name>
  <files>docs/PRE_REGISTRATION.md, external_data_adapter.py</files>
  <action>
1. Create `docs/PRE_REGISTRATION.md` with ALL locked parameters from Phase 4:
   - Features: parietal_theta_rel, parietal_alpha_rel, alpha_asym_frontal, parietal_TBR, riem_central_temporal (5 dims)
   - Model: LogisticRegression(C=0.1, class_weight="balanced", max_iter=1000, random_state=42)
   - Scaler: StandardScaler (fit on MODMA only)
   - Preprocessing: highpass 1.0Hz, lowpass 45Hz, resample 125Hz, avg ref, QC 200uV, window 10s (skip W0), crop 60s, interp if <=25% bad
   - Evaluation: BA>0.60, CI lower>0.50, p<0.05, 1000 permutations on external test set
   - Adaptation whitelist: file format adapter, channel mapping, resampling only
   - Explicitly forbidden: changing QC thresholds, features, model/hyperparams, evaluation criteria

2. Create `external_data_adapter.py` with two functions:
   - `load_tdbrain_subjects(tdbrain_root)`: Parse participants.tsv, filter MDD+HC, return DataFrame. Log counts. If no HC found, raise ValueError with fallback instructions.
   - `load_tdbrain_features(tdbrain_root, resample_sfreq=125, crop_duration=60, highpass_freq=1.0, bad_amp_uv=200, window_sec=10, min_windows_per_subject=3)`:
     For each subject: `mne.io.read_raw_bdf()` -> pick only channels in STANDARD_1020_REGION_MAP -> set standard_1020 montage -> crop -> filter 1.0-45Hz -> avg ref -> resample -> window (skip W0) -> per-window QC (200uV, 12% dynamic bad) -> interpolate if <=25% bad.

     **Post-resample spectrum sanity check (REQUIRED by locked decision, CONTEXT.md section 4):**
     After resampling the FIRST subject, compute Welch PSD (scipy.signal.welch) on one occipital/parietal channel.
     - Log the peak frequency in the 8-12Hz alpha band. Warn if peak is outside 8-13Hz range.
     - Check for power spike near 62.5Hz (Nyquist/2). Warn if power at 60-62.5Hz exceeds median power by >10x, indicating aliasing artifact.
     - This is a one-time diagnostic log, not a gate — processing continues regardless, but warnings are logged for manual review.

     After windowing all subjects, call `extract_features(X, sfreq, ch_names)` from modma_mdd_real_experiment.py.
     Return (features, y, groups, ch_names, qc_stats_dict).
   - Import from modma_mdd_real_experiment: extract_features, build_region_indices, STANDARD_1020_REGION_MAP, build_quality_mask.
   - Add a `if __name__ == "__main__"` block that runs on synthetic dummy data (random arrays) to verify the adapter code path works without real TDBRAIN data. This is critical — no real external data before git tag.
  </action>
  <verify>
    python -c "from external_data_adapter import load_tdbrain_subjects, load_tdbrain_features; print('imports OK')"
    python external_data_adapter.py  (runs dummy self-test, should print success without needing real data)
  </verify>
  <done>PRE_REGISTRATION.md contains all locked params. external_data_adapter.py imports successfully, includes post-resample spectrum sanity check, and self-test passes on dummy data.</done>
</task>

<task type="auto">
  <name>Task 2: Git-tag pre-registration</name>
  <files></files>
  <action>
1. Stage and commit docs/PRE_REGISTRATION.md and external_data_adapter.py with message: "docs(05): pre-register confirmatory replication pipeline"
2. Create annotated git tag: `git tag -a pre-registration-v1 -m "Locked pipeline for confirmatory replication. No external data has touched the classifier."`
3. Verify tag exists: `git tag -l "pre-registration-v1"`

CRITICAL: This tag MUST exist before Plan 02 runs. Plan 02 will train on MODMA and test on external data — that is only valid if the pipeline was locked beforehand.
  </action>
  <verify>
    git tag -l "pre-registration-v1" returns the tag name
    git log --oneline -1 shows the pre-registration commit
  </verify>
  <done>Git tag "pre-registration-v1" exists on a commit containing PRE_REGISTRATION.md and external_data_adapter.py. No external data has been processed.</done>
</task>

</tasks>

<verification>
- docs/PRE_REGISTRATION.md exists with all 5 feature names, model params, and evaluation criteria
- external_data_adapter.py imports from modma_mdd_real_experiment without error
- external_data_adapter.py includes post-resample Welch PSD sanity check (alpha peak + Nyquist artifact detection)
- external_data_adapter.py self-test passes on dummy data
- Git tag "pre-registration-v1" exists
- No real TDBRAIN data has been downloaded or processed
</verification>

<success_criteria>
Pre-registration is committed and tagged. Data adapter is ready to load TDBRAIN when data becomes available, with spectrum sanity check after resampling. No external data has touched the classifier.
</success_criteria>

<output>
After completion, create `.planning/phases/05-confirmatory-replication-with-pre-registered-pipeline/05-01-SUMMARY.md`
</output>
