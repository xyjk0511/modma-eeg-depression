---
phase: 16-preprocessing-deferred
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/modma/modma_mdd_real_experiment.py
autonomous: true
requirements:
  - PRE-02

must_haves:
  truths:
    - "pyprep NoisyChannels replaces amplitude-only bad channel detection in load_windows()"
    - "Subjects with >20% bad channels excluded (adaptive relaxation to 25%/30% if retention <35)"
    - "A/B comparison report shows old vs pyprep BA, AUC, subject counts, bad channel stats"
    - "If pyprep BA < threshold, code falls back to old method automatically"
  artifacts:
    - path: "scripts/modma/modma_mdd_real_experiment.py"
      provides: "pyprep NoisyChannels in load_windows(), A/B runner"
      contains: "NoisyChannels"
  key_links:
    - from: "load_windows()"
      to: "pyprep.NoisyChannels"
      via: "nc.find_all_bads(ransac=True)"
      pattern: "NoisyChannels.*find_all_bads"
---

<objective>
Integrate pyprep NoisyChannels into resting-state pipeline load_windows(), replacing amplitude-only bad channel detection with PREP-standard multi-criteria detection. Add adaptive exclusion threshold and A/B comparison runner.

Purpose: Close PRE-02 — upgrade from amplitude-only to PREP standard bad channel detection.
Output: Updated modma_mdd_real_experiment.py with pyprep path, A/B comparison output.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-preprocessing-deferred/16-RESEARCH.md
@.planning/phases/16-preprocessing-deferred/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate pyprep NoisyChannels into load_windows()</name>
  <files>scripts/modma/modma_mdd_real_experiment.py</files>
  <action>
1. Add `from pyprep import NoisyChannels` at top of file.

2. Add `use_pyprep` param to `load_windows()` (default=True). Keep existing amplitude detection as fallback.

3. Inside load_windows(), after highpass filter (line 251), before current amplitude detection (lines 253-256), add conditional branch:

   If use_pyprep=True:
   - `nc = NoisyChannels(raw, do_detrend=False, random_state=42)` (do_detrend=False because already highpass at 1.0 Hz)
   - `nc.find_all_bads(ransac=True, channel_wise=False)`
   - `bad_chs = nc.get_bads()`

   If use_pyprep=False:
   - Keep existing amplitude detection unchanged

4. Replace fixed 25% exclusion (line 260) with `max_bad_pct` param (default=0.20). Exclude if `len(bad_chs) > int(n_ch * max_bad_pct)`.

5. Log per-subject: sub_id, n_bad, bad channel names, detection method.

6. Add `use_pyprep` and `max_bad_pct` to `_compute_cache_key()` for cache invalidation.

7. Add `--use-pyprep` (store_true, default True) and `--max-bad-pct` (float, default 0.20) CLI args in parse_args().
  </action>
  <verify>
Run with --no-cache --use-pyprep: completes, prints per-subject bad channel logs.
Run with --no-use-pyprep: uses old amplitude method.
  </verify>
  <done>
load_windows() uses pyprep by default; old path behind --no-use-pyprep; per-subject log printed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adaptive threshold + A/B comparison runner</name>
  <files>scripts/modma/modma_mdd_real_experiment.py</files>
  <action>
1. Adaptive threshold: wrap load_windows call in loop over [0.20, 0.25, 0.30]. Break when retained subjects >= 35. Log which threshold used.

2. Add `run_ab_comparison(args)`:
   - Run pipeline twice: use_pyprep=True and use_pyprep=False
   - Record BA, AUC, n_subjects, n_mdd, n_hc, mean bad channels
   - Print structured comparison table
   - Save to results/pyprep_ab_comparison.json

3. Regression gate (resting-state baseline BA=0.613):
   - Primary: pyprep BA >= 0.613
   - Secondary: if 0.580 <= BA < 0.613, accept only if retention AND balance improved
   - If neither: print warning, recommend --no-use-pyprep

4. Add `--ab-compare` CLI flag triggering run_ab_comparison().
  </action>
  <verify>
Run --ab-compare --no-cache: prints A/B table, saves JSON.
  </verify>
  <done>
Adaptive threshold retries 20-25-30% for >=35 subjects. A/B report with BA, AUC, counts. Regression gate warns on degradation.
  </done>
</task>

</tasks>

<verification>
1. `python scripts/modma/modma_mdd_real_experiment.py --bids-root data/modma --no-cache --ab-compare` completes and produces results/pyprep_ab_comparison.json
2. Both paths retain >= 35 subjects
3. Per-subject bad channel log shows method and channel names
4. Adaptive threshold log shows which threshold used
</verification>

<success_criteria>
- pyprep NoisyChannels integrated with all 4 detection methods
- Old amplitude detection preserved as fallback
- Adaptive threshold ensures >= 35 subjects
- A/B comparison report generated
- Regression gate applied
</success_criteria>

<output>
After completion, create `.planning/phases/16-preprocessing-deferred/16-01-SUMMARY.md`
</output>
