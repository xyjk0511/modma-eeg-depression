---
phase: 16-preprocessing-deferred
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - scripts/modma/run_modma_erp.py
  - scripts/modma/modma_mdd_real_experiment.py
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "ERP load_erp_features() and load_erp_timeseries() cache results as .npz"
    - "Second ERP run loads from cache in <1s (no .raw re-read)"
    - "Cache key includes filter params hash; param change invalidates cache"
    - "Resting-state _CACHE_VERSION bumped to 3 after pyprep integration"
    - "--no-cache flag skips ERP cache"
  artifacts:
    - path: "scripts/modma/run_modma_erp.py"
      provides: "ERP .npz caching for load_erp_features and load_erp_timeseries"
      contains: "_erp_cache_path"
    - path: "scripts/modma/modma_mdd_real_experiment.py"
      provides: "Bumped _CACHE_VERSION = 3"
      contains: '_CACHE_VERSION = "3"'
  key_links:
    - from: "load_erp_features()"
      to: "data/modma/cache/*.npz"
      via: "np.savez / np.load"
      pattern: "np\.savez|np\.load"
---

<objective>
Add .npz caching to ERP pipeline (load_erp_features, load_erp_timeseries) and bump resting-state cache version after pyprep integration.

Purpose: Close ARCH-01 — ERP pipeline re-reads .raw files every run (~minutes); cache cuts to <1s.
Output: Cached ERP loading, bumped resting-state cache version.
</objective>

<execution_context>
@C:/Users/55093/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/55093/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-preprocessing-deferred/16-RESEARCH.md
@.planning/phases/16-preprocessing-deferred/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .npz cache to ERP loading functions</name>
  <files>scripts/modma/run_modma_erp.py</files>
  <action>
1. Add `import hashlib` at top. Define `_ERP_CACHE_VERSION = "1"` and `ERP_CACHE_DIR = ROOT / "data/modma/cache"`.

2. Add helper `_erp_cache_path(condition, func_name, raw_files)`:
   - SHA-256 of: _ERP_CACHE_VERSION + condition + func_name + FMIN + FMAX + TMIN + TMAX + P300_WIN + N200_WIN + sorted raw file mtimes
   - Return `ERP_CACHE_DIR / f"{hash[:16]}.npz"`

3. In `load_erp_features()`:
   - At start: compute cache path. If exists, `np.load` and return (X, y, ids).
   - At end (before return): `np.savez(cache_path, X=X, y=y, ids=ids)`.

4. In `load_erp_timeseries()`:
   - At start: compute cache path. If exists, load and return (feat, erps, times, labels, ids).
   - At end: savez with all arrays.

5. Add module-level `_NO_CACHE = False` flag. Check `--no-cache` in sys.argv at module load or pass via a setter. Simplest: add `set_erp_cache(enabled)` function called from __main__.

6. Ensure cache dir created with `os.makedirs(ERP_CACHE_DIR, exist_ok=True)` before save.
  </action>
  <verify>
First run: prints "Cache miss", processes all .raw files, saves .npz.
Second run: prints "Cache hit", returns in <1s.
Run with --no-cache: skips cache, processes .raw files fresh.
  </verify>
  <done>
load_erp_features() and load_erp_timeseries() cache to .npz. Second run <1s. --no-cache bypasses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump resting-state cache version</name>
  <files>scripts/modma/modma_mdd_real_experiment.py</files>
  <action>
1. Change `_CACHE_VERSION = "2"` to `_CACHE_VERSION = "3"` in modma_mdd_real_experiment.py (line 79).
   This invalidates old caches so pyprep-processed data is used instead of stale amplitude-only cached windows.

2. Verify the cache key function `_compute_cache_key()` already includes `use_pyprep` and `max_bad_pct` from Plan 16-01. If not, add them.
  </action>
  <verify>
Confirm `_CACHE_VERSION = "3"` in file. Run resting-state pipeline with cache enabled — should rebuild cache (no stale hit).
  </verify>
  <done>
_CACHE_VERSION bumped to "3". Old caches invalidated. New runs produce pyprep-processed cached data.
  </done>
</task>

</tasks>

<verification>
1. First ERP run processes .raw files and saves .npz cache
2. Second ERP run loads from cache in <1s
3. --no-cache flag bypasses cache
4. _CACHE_VERSION = "3" in resting-state script
5. ERP BA=0.670 unchanged (cache is transparent)
</verification>

<success_criteria>
- ERP .npz cache works for both load_erp_features and load_erp_timeseries
- Second run <1s confirmed
- Resting-state cache version bumped to 3
- --no-cache flag supported
</success_criteria>

<output>
After completion, create `.planning/phases/16-preprocessing-deferred/16-02-SUMMARY.md`
</output>
